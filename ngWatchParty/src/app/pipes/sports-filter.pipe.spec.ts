import { SportsFilterPipe } from './sports-filter.pipe';

describe('SportsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SportsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
